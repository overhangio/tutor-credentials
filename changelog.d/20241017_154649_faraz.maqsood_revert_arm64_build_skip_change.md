- [Feature] Remove IMAGES_BUILD_PLATFORM setting. This setting was initially added to prevent the credentials image from being built for arm64 due to issues with the didkit package on arm64 machines. Now, the image can be built for both amd64 and arm64 architectures. (by @Faraz32123)
